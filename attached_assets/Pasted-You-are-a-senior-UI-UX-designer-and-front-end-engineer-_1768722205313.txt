You are a senior UI/UX designer and front-end engineer.

TASK:
Redesign the mobile hero section of Yogesh Yadav’s website to feel premium, distinctive, and intentional.

CURRENT ISSUES TO FIX:
1. Profile image feels like it’s floating separately.
2. Image does not integrate into the hero layout.
3. CTAs risk going below the fold on some devices.
4. Hero looks like a standard template instead of a crafted personal brand.

PRIMARY GOALS:
- Integrate Yogesh Yadav’s photo into the hero background using a curved / organic shape.
- Ensure BOTH CTAs (“Work with me” and “Read my thinking”) are fully visible above the fold on mobile.
- Remove ALL vertical scrolling on first load (even slight scroll).
- Make the hero section visually memorable and clean.

--------------------------------------------------
HERO IMAGE DESIGN REQUIREMENTS
--------------------------------------------------

IMAGE TREATMENT:
- Do NOT use a simple circle avatar.
- Use an organic curved mask / arc / soft wave shape for the image.
- The image should feel “cut into” the hero background.
- The curve should resemble a hand-drawn arc or flowing line (like the one shown in the reference screenshot).
- The photo should visually anchor the hero, not float.

IMPLEMENTATION OPTIONS (choose best):
- CSS clip-path (preferred)
- SVG mask with smooth curves
- Pseudo-element overlay with curved border
- Subtle gradient fade at image edges

STYLE:
- Soft shadow, not harsh
- No hard borders
- Neutral background
- Image slightly overlaps hero background for depth

--------------------------------------------------
LAYOUT RULES (MOBILE-FIRST)
--------------------------------------------------

- Entire hero must fit in 100vh on mobile.
- No vertical scrolling required.
- Vertical rhythm must be tight but breathable.
- Image at top, text follows naturally.
- CTAs must be visible WITHOUT scrolling.

Use:
- Reduced padding
- Compact but readable typography
- Proper line-height, not oversized margins

--------------------------------------------------
CTA PLACEMENT (VERY IMPORTANT)
--------------------------------------------------

- Place BOTH CTAs inside the hero.
- “Work with me” = primary button
- “Read my thinking” = secondary button
- Buttons stacked vertically on mobile
- Adequate spacing, no crowding
- Buttons must NOT push content below viewport

--------------------------------------------------
HERO CONTENT STRUCTURE
--------------------------------------------------

1. Integrated Image Section (with curved mask)
2. Availability pill (“Available for new projects”)
3. Headline (keep exactly as is):
   “I help consumer & B2B products grow revenue, retention, and scale.”
4. Subheadline:
   Yogesh Yadav — Product Leader with 9+ years across Fintech, Mobility, SaaS & Marketplaces.
5. CTA buttons (both visible)

--------------------------------------------------
VISUAL POLISH
--------------------------------------------------

- Add subtle background gradient or noise texture
- No loud colors
- Primary accent stays blue
- Smooth transitions (no animation required)
- Professional, calm, confident tone

--------------------------------------------------
TECH REQUIREMENTS
--------------------------------------------------

- Mobile-first CSS
- Responsive across common screen heights (iPhone SE to Pro Max)
- Use clamp() for font sizes where needed
- No JS required
- Clean, commented HTML & CSS

--------------------------------------------------
OUTPUT
--------------------------------------------------

- Updated hero HTML
- Updated hero CSS
- Clear comments explaining:
  - Image masking approach
  - Fold control decisions
  - CTA placement logic

DO NOT:
- Use a circular avatar
- Push CTAs below the fold
- Add unnecessary animations
- Break readability

DESIGN THIS LIKE A HIGH-END PERSONAL BRAND, NOT A TEMPLATE.
